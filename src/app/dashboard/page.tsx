export default function DashboardPage() {
  // Placeholder — replaced in P6 with baby list + redirect logic.
  return (
    <main className="mx-auto w-full max-w-2xl px-4 py-12">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
    </main>
  );
}
