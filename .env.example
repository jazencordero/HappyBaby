NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_URL=

# Server-only. Outbound invitation email via Resend (resend.com). If unset,
# invitations still work — the copyable link is the fallback and always
# works regardless of email delivery. Never prefix NEXT_PUBLIC_.
RESEND_API_KEY=
